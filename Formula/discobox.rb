# Generated by `task brew:formula`. Do not edit by hand.
class Discobox < Formula
  desc "Sandboxed development environments for coding agents"
  homepage "https://github.com/discobox-ai/discobox"
  version "0.1.0-alpha.3"
  license "Apache-2.0"

  on_macos do
    # Apple Silicon only. ADR 0062 defers Intel Macs and
    # assemble-guest-image refuses non-arm64, so an Intel binary would
    # install and then fail at the first pool.
    on_arm do
      url "https://github.com/discobox-ai/discobox/releases/download/v0.1.0-alpha.3/discobox-darwin-arm64"
      sha256 "232125fb60d7fe279758376a7435957fd6540e8feeb26b3f4d4e3c2fa5c47f67"
    end
  end

  on_linux do
    on_intel do
      url "https://github.com/discobox-ai/discobox/releases/download/v0.1.0-alpha.3/discobox-linux-amd64"
      sha256 "fa3ff9eab87d6e5da7f293dddbdb21ebf761347b66ecb55bf87ce2c7314f0491"
    end
    on_arm do
      url "https://github.com/discobox-ai/discobox/releases/download/v0.1.0-alpha.3/discobox-linux-arm64"
      sha256 "297a56b2a037f74665dd5401de4d97862279453b4bc837ac5154fea97f5f733c"
    end
  end

  def install
    bin.install Dir["discobox-*"].first => "discobox"
  end

  def caveats
    on_macos do
      <<~EOS
        This binary is ad-hoc signed with com.apple.security.virtualization,
        which Virtualization.framework requires to create a VM. Without it a
        macOS pool cannot start.

        com.apple.vm.networking is deliberately absent. It is what puts guests
        on your LAN, and Apple grants it by request; pool guests are NAT'd by
        the framework instead.
      EOS
    end
  end

  test do
    # Proves both that the binary runs on the platform it was installed
    # for — what a cross-compiled release most plausibly gets wrong —
    # and that it is the version this formula claims.
    assert_match version.to_s, shell_output("#{bin}/discobox --version")
  end
end
